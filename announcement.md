Version 1.521G released.
- Fixed a bug of fontset configuration for `upTeX`: replace the bold type JFM name `upzhserifitb` with the default fallback one
- Optimized the `ctex-spa-lxgw.tex` script.
- Make the bold version of the main font heavier
- Optimized the `build.lua` script, replaced some lines to interface provided by `l3build`; Enabled the tagging function for bumping easily
- Added Index in the manual